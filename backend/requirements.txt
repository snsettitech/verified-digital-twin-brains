fastapi
uvicorn
supabase
pinecone
openai

# Starter-plan safe reranking dependencies are installed by default so
# Render's default build command (pip install -r requirements.txt) keeps
# Cohere/FlashRank available in production.
flashrank
cohere

# Additional optional embedding dependencies:
#   requirements-ml-local.txt  (heavy local HF model runtime)

# Cerebras for fast inference (10x faster than OpenAI)
cerebras-cloud-sdk>=1.0.0
pydantic
python-dotenv
python-multipart

# ---------- Agent Runtime ----------
# ADK migration: LangChain deps kept for backward compatibility during
# migration. Set AGENT_RUNTIME=adk to use the new pipeline.
# Once migration is complete, remove the langchain block below.
langchain
langchain-openai
langchain-community
langgraph
langgraph-checkpoint-postgres

# ADK: New agent framework (Phase 1: foundation alongside LangGraph)
# google-adk>=1.0.0
# litellm>=1.50.0

asyncpg
PyPDF2
python-jose[cryptography]
passlib[bcrypt]
composio-langchain
httpx
yt-dlp
feedparser
twikit
youtube-transcript-api
google-genai
google-api-python-client
beautifulsoup4
elevenlabs
firecrawl-py
exa-py>=1.0.0
apscheduler
# Zep/Graphiti memory layer
graphiti-core>=0.5.0
neo4j>=5.26.0
python-docx
openpyxl
pydub
imageio-ffmpeg
redis
langfuse>=2.0.0
